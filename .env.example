# ========================================
# LIVE ACADEMIA - Environment Variables
# ========================================

# IMPORTANTE: Copie este arquivo para .env.local e preencha com valores reais
# NUNCA commite .env.local no Git!

# ========================================
# Supabase (Database)
# ========================================
NEXT_PUBLIC_SUPABASE_URL=https://seu-projeto.supabase.co
SUPABASE_SERVICE_ROLE_KEY=sua-service-role-key-aqui

# ========================================
# Encryption (API Keys)
# ========================================
# Segredo para criptografia AES-256-GCM (mínimo 32 caracteres)
# Gere com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ENCRYPTION_SECRET=seu-segredo-forte-aqui-minimo-32-chars-nunca-commite-isso

# ========================================
# Sanity CMS
# ========================================
NEXT_PUBLIC_SANITY_PROJECT_ID=c9pbklm2
NEXT_PUBLIC_SANITY_DATASET=production
SANITY_API_TOKEN=seu-sanity-api-token-aqui

# Webhook de revalidação (para sincronização automática Sanity → Site)
# Gere com: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SANITY_REVALIDATE_SECRET=token-secreto-para-webhook-minimo-32-chars
SANITY_WEBHOOK_SECRET=outro-token-diferente-opcional-mas-recomendado

# ========================================
# Pacto API (Payment Processing)
# ========================================
# URL base da API Pacto (opcional, tem valor padrão)
PACTO_API_URL=https://apigw.pactosolucoes.com.br

# NOTA: As chaves de API da Pacto estão armazenadas CRIPTOGRAFADAS
# na tabela 'units' do Supabase. Não coloque chaves aqui!

# ========================================
# Analytics & Tracking
# ========================================
# Google Analytics 4
NEXT_PUBLIC_GA4_ID=G-XXXXXXXXXX

# Google Tag Manager
NEXT_PUBLIC_GTM_ID=GTM-XXXXXXX

# Meta Pixel (Facebook)
NEXT_PUBLIC_META_PIXEL_ID=123456789012345

# ========================================
# Site Configuration
# ========================================
# URL do site (usado para analytics e canonical URLs)
NEXT_PUBLIC_SITE_URL=https://liveacademia.com.br

# Ambiente (development, staging, production)
NEXT_PUBLIC_ENV=development

# ========================================
# INSTRUÇÕES
# ========================================
#
# 1. DESENVOLVIMENTO LOCAL:
#    cp .env.example .env.local
#    Preencha com valores reais
#
# 2. PRODUÇÃO (Vercel):
#    Configure todas as variáveis no dashboard do Vercel
#    Project Settings → Environment Variables
#
# 3. SECRETS IMPORTANTES:
#    - ENCRYPTION_SECRET: Usado para criptografar chaves de API no DB
#    - SANITY_REVALIDATE_SECRET: Protege endpoint de revalidação
#    - SUPABASE_SERVICE_ROLE_KEY: Acesso total ao banco
#
#    NUNCA exponha estes valores publicamente!
#
# 4. VARIÁVEIS PÚBLICAS:
#    Variáveis com prefixo NEXT_PUBLIC_ são expostas no browser
#    Nunca coloque secrets nessas variáveis!
#
# 5. SANITY WEBHOOKS:
#    Para configurar webhooks de revalidação automática:
#    Veja: docs/SANITY-WEBHOOK-SETUP.md
#
# ========================================
