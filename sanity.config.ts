// Root Sanity config used for hosted Studio deployment (sanity deploy)
// Reusa a configuração já definida em ./sanity/config.ts (embed utilizada no Next.js)
import config from './sanity/config'
export default config
